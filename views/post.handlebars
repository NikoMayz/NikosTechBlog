<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} - Niko's Tech Blog</title>
  <link rel="stylesheet" href="../../public/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex flex-column align-center p-2">
      <nav class="display-flex justify-flex-start">
        <a href="/">Home</a> |
        <a href="/dashboard">Dashboard</a> |
        {{#if logged_in}}
        <button class="no-button" id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
      <h1>Niko's Tech Blog</h1>
    </header>
    <main class="container">
      <h2>{{ title }}</h2>
      <p>{{ description }}</p>
      <p>Created by: {{ user.userName }}</p>
      <p>Posted on: {{ format_date createdAt }}</p>

      <!-- Comments Section -->
      <div class="comments">
        <h4>Comments</h4>
        {{#each comments}}
        <div class="comment">
          <p>{{ this.text }}</p>
          <p>Commented by: {{ this.user.userName }} on {{ format_date this.createdAt }}</p>
        </div>
        {{else}}
        <p>No comments yet.</p>
        {{/each}}
        
        <!-- Comment Form for Logged-in Users -->
        {{#if logged_in}}
        <form action="/posts/{{id}}/comments" method="POST">
          <textarea name="comment" placeholder="Add a comment" required></textarea>
          <button type="submit">Submit</button>
        </form>
        {{/if}}
      </div>
    </main>
    <footer>
      <!-- Footer content here -->
    </footer>
  </div>

  <!-- Include script for logout functionality if logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
